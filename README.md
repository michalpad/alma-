# ALMA — מורה AI למתמטיקה ליסודי

שרת FastAPI. הפעלה:
- `uvicorn almapkg.app:app --host 0.0.0.0 --port $PORT`

משתני סביבה נדרשים:
- `GEMINI_API_KEY` — מפתח Gemini
- `ALMA_FIELD_KEY` — מפתח הצפנת שדות
